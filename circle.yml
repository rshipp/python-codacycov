machine:
  python:
    version: 3.4.3

dependencies:
  post:
    - sudo pip install pep8 --upgrade
    - sudo pip install pyflakes
    - sudo pip install coverage
    
test:
  override:
    - make test
    - make install
    - make coverage