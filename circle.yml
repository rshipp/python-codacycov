machine:
  python:
    version: 2.7.10

dependencies:
  post:
    - sudo pip install pep8 --upgrade
    - sudo pip install pyflakes
    - sudo pip install coverage
    
test:
  override:
    - make test
    - make install
    - make coverage